# Firebase's package.json has a known typo in some versions ("dependency"
# instead of "dependencies") that breaks module resolution under pnpm's
# strict node_modules when packages aren't hoisted. Hoisting @firebase/*
# works around it. See: firebase/firebase-js-sdk issue #9787
public-hoist-pattern[]=*firebase*
